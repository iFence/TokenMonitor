# Changelog

本文件维护每个版本对外发布的更新说明。

编写约定：
- 每个版本使用 `## v版本号` 作为标题，例如 `## v0.2.1`
- 标题下方直接填写会展示在应用更新弹窗中的 Markdown 内容
- 使用 `---` 或下一个版本标题结束当前版本说明

## v0.2.6

### ✨ 新增功能
- **日报页面**：新增「报告」页，展示过去 365 天的 Token 用量总结统计与贡献式热力图，支持悬停查看单日用量的详细信息。

### 🚀 优化改进
- 修复 GPUI 颜色色相归一化范围（0..=1）问题，图表与热力图配色更准确。

---

## v0.2.4

### ✨ 新增功能
- **支持 Antigravity 用量统计**：读取 `~/.gemini/antigravity-cli/` 与 `~/.gemini/antigravity-ide/` 下各会话 SQLite 库中模型生成步骤的 Token 用量（含输入/输出/缓存读），同时覆盖 Antigravity CLI 与 IDE；Windows 下自动发现并纳入各 WSL 发行版中的会话，与本地数据去重隔离。
- **支持 WorkBuddy 用量统计**：读取 `~/.workbuddy/projects/**/*.jsonl` 会话记录中的 `message.usage` / `providerData.usage`，兼容新旧字段命名，纳入 Token 用量与成本统计。

---

## v0.2.3

### ✨ 新增功能
- **支持 Pi 用量统计**：读取 `~/.pi/agent/sessions/**/*.jsonl` 会话记录中的 assistant 消息、工具内 LLM 调用与压缩/分支摘要用量；Windows 下自动发现并纳入各 WSL 发行版中的 Pi 会话，与本地会话去重隔离。

---

## v0.2.2

### ✨ 新增功能
- **可配置扫描间隔**：设置中可选择 30 秒到 1 小时的自动扫描间隔，修改后立即生效。
- **增量文件扫描**：按文件修改时间与大小跳过未变化的数据源，降低定时重扫的 IO 与内存开销。
- **便携版自动更新**：便携版构建也可检查并应用 GitHub Releases 更新。
- **Windows 深色标题栏**：窗口标题栏跟随深色主题。
- **应用图标与开始菜单快捷方式**：安装包附带应用图标，并注册开始菜单入口。

### 🐛 修复
- 修复图表页顶部刻度超出绘图区域的问题。
- 修复修改扫描间隔后未立即生效、需等待旧周期结束的问题。

### 🚀 优化改进
- 勾选/取消勾选或排序应用时，仅在启用新应用时才触发扫描，其余情况仅刷新视图，避免冗余扫描。

---

## v0.2.1

### ✨ 新增功能
- **OpenCode 适配器**：解析 OpenCode 的会话数据，纳入 Token 用量与成本统计。
- **OpenClaw 适配器**：解析 OpenClaw 的会话数据，纳入 Token 用量与成本统计。
- **DeepSeek Harness 适配器**：解析 `~/.dsh` 下 zstd 压缩的会话数据，纳入用量与成本统计。
- **图表时间范围改为下拉选择**：图表页的时间范围从标签页切换为下拉菜单，更紧凑直观。

---

## v0.2.0

### ✨ 新增功能
- **首个正式发布**：聚合展示 Claude Code、Codex、Gemini CLI、CodeBuddy、OpenCode、Qoder 等 AI 编程工具的 Token 用量与成本。
- **多维度聚合**：按应用、模型、项目、日期聚合展示，支持日/周/月/年时间窗口。
- **图表页**：按天查看用量与成本趋势，支持应用/模型筛选与指标切换。
- **成本计算**：内置 300+ 模型定价，按输入/输出/缓存读写归因成本。
- **应用选择设置**：可启用/禁用并排序要追踪的应用。
- **WSL 合并统计**：自动发现并合并 WSL 发行版中的用量数据。
- **自动更新检查**：启动时检查 GitHub Releases 是否有新版本，在「关于」面板提示。

### 🚀 优化改进
- 大幅降低每 30 秒定时重扫的内存与 IO 开销（指纹跳过无变化数据源 + 聚合下沉 SQL）。

---
